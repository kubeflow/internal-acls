// package gcs provides useful utilities for GCS
// This package isn't specific to our groups sync. We should probably move it to a more generic location
// and make it its own go module to make it easy to reuse.
package gcs
